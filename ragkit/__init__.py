"""ragkit — a RAG toolkit built up one lesson at a time.

Read `lessons/00-setup/README.md` first. Every lesson wires a real module into this
package, so by the end the lessons *are* the production service.
"""

__version__ = "0.1.0"
